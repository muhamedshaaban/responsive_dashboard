import 'package:flutter/material.dart';

class IncomeItemModel {
  final String title, progress;
  final Color color;

  const IncomeItemModel({required this.title, required this.progress, required this.color});
}